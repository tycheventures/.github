# Fix testimonials, service redirects, and profile logo

## 1. Add the missing testimonials

The archived homepage carries 16 testimonials; the site currently shows 9. Add the 7 missing ones (in the archive's order) to the testimonials data:

- Dinesh Chandak — no company, no link
- Siddharth Dharmadhikari — Chanakya Manadal, links to chanakyamandal.org
- Abdirisak Mohamed — Top Somali Jobs (topsomalijobs.so)
- Alexander Weitnauer — MD Ergotopia Portal (ergotopia-portal.de)
- Ajay Domun — MD Magna Carta Ltd and MD Florian Kappe LLP (magnacartatranslation.com)
- Carina Kappe — MD Florian Kappe LLP (floriankappe.com)
- Jana Novak — Cubio Design (cubiodesign.com)

Also restore the full text of the Georg Meyer quote, which is currently truncated compared to the archive.

Portrait images for these people will be downloaded from the archive into the project's image folder, so nothing loads from a live URL. Testimonial order will follow the archive.

## 2. Redirect the individual service (and work) pages

Today only the top-level `/service/`, `/services/`, and `/work/` stubs exist in the exported site, so a deep link such as `/service/seo/` returns a 404 on GitHub Pages. Fix by generating a redirect stub for every deep link the site points at:

- Each footer service link (`/service/website-designing/`, `/service/dynamic-website-development/`, `/service/domain-and-hosting/`, `/service/graphics-design/`, `/service/seo/`, `/service/digital-marketing/`, `/services/annual-maintenance-contract/`)
- Each homepage service card link (`/services/...`)
- Each portfolio project link (`/work/...`)

Every stub redirects to its matching URL on the live WordPress site; if a specific target no longer exists there, it falls back to the main Services page. A `404.html` fallback that redirects to the live site is added too, so any path missed still lands somewhere sensible.

## 3. Fix the broken logo in profile/README.md

The README currently hotlinks the logo from tycheventures.com. Copy `logo.png` into the `profile/` folder and reference it with a relative path (`./logo.png`) so it renders from the repository itself. The link around it still opens the live site in a new tab.

## Technical notes

- Data changes in `src/lib/site-data.ts`; new avatars saved under `public/img/`.
- Redirect stubs generated by the existing export script so `docs/` stays reproducible; each stub uses a meta refresh + canonical + `location.replace`, matching the current stub format.
- Testimonial slider markup is unchanged — only the data grows.
- Rebuild `docs/` after the changes so the static export matches the React app.
